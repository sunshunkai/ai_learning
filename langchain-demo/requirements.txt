# LangChain 依赖
# 核心 + DeepSeek（OpenAI 兼容接口）
langchain>=1.0
langchain-openai>=0.3
# 16_skill_loading.py 的 --mode anthropic 使用
langchain-anthropic>=1.7.2
langchain-text-splitters>=0.3
numpy>=1.26
# 其余 demo 仍通过 langchain-openai 接入 DeepSeek
# 支持从项目根 .env 文件读取 API Key（配合 tools/load_env.py 使用）
python-dotenv>=1.0.0
